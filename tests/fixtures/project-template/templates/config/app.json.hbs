{
  "host": "{{config.host}}",
  "port": {{config.port}},
  "debug": {{config.debug}},
  "database": {
    "type": "{{database.type}}",
    "host": "{{database.host}}",
    "port": {{database.port}},
    "name": "{{database.name}}"
  },
  "features": {
    "auth": {{features.auth}},
    "api": {{features.api}},
    "frontend": {{features.frontend}}
  }
}
