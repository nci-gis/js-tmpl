# Generated by js-tmpl
# Project: my-app v1.0.0
# Environment: development

database:
  type: postgresql
  connection:
    host: db.example.com
    port: 5432
    database: myapp_db

  credentials:
    username: app_user
    # Password should be injected via environment variable
    password_env: DB_PASSWORD

  pool:
    min: 2
    max: 10
    timeout: 30000

# ---
# Maintained by: DevOps Team
